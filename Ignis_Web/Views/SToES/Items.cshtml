@{
    ViewBag.Title = "Itemy";
}
<h2>@ViewBag.Title - Świątynia Słońca Wiecznego Snu</h2>
<div class="jumbotron jumbotron-opacity">
    <div class="row">


        <div class="col">
            <h3>Płytowe</h3>
            <table class="table table-striped table-dark table-max-width table-sortable" data-sorting="true">
                <thead>
                    <tr>
                        <td data-type="text">Nick</td>
                        <td data-type="text" data-sorted="true" data-direction="ASC">Przedmiot</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in ViewBag.listPlate)
                    {
                        <tr>
                            <td>@item.Nickname</td>
                            <td>@item.Name</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="col">
            <h3>Kolcze</h3>
            <table class="table table-striped table-dark table-max-width table-sortable" data-sorting="true">
                <thead>
                    <tr>
                        <td data-type="text">Nick</td>
                        <td data-type="text" data-sorted="true" data-direction="ASC">Przedmiot</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in ViewBag.listChain)
                    {
                        <tr>
                            <td>@item.Nickname</td>
                            <td>@item.Name</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="col">
            <h3>Magowe</h3>
            <table class="table table-striped table-dark table-max-width table-sortable" data-sorting="true">
                <thead>
                    <tr>
                        <td data-type="text">Nick</td>
                        <td data-type="text" data-sorted="true" data-direction="ASC">Przedmiot</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in ViewBag.listCloth)
                    {
                        <tr>
                            <td>@item.Nickname</td>
                            <td>@item.Name</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="col">
            <h3>Skórzane</h3>
            <table class="table table-striped table-dark table-max-width table-sortable" data-sorting="true">
                <thead>
                    <tr>
                        <td data-type="text">Nick</td>
                        <td data-type="text" data-sorted="true" data-direction="ASC">Przedmiot</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in ViewBag.listLeather)
                    {
                        <tr>
                            <td>@item.Nickname</td>
                            <td>@item.Name</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="col">
            <h3>Leczykowe</h3>
            <table class="table table-striped table-dark table-max-width table-sortable" data-sorting="true">
                <thead>
                    <tr>
                        <td data-type="text">Nick</td>
                        <td data-type="text" data-sorted="true" data-direction="ASC">Przedmiot</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in ViewBag.listHeal)
                    {
                        <tr>
                            <td>@item.Nickname</td>
                            <td>@item.Name</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="col">
            <h3>Bronie</h3>
            <table class="table table-striped table-dark table-max-width table-sortable" data-sorting="true">
                <thead>
                    <tr>
                        <td data-type="text">Nick</td>
                        <td data-type="text" data-sorted="true" data-direction="ASC">Przedmiot</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in ViewBag.listWeapon)
                    {
                        <tr>
                            <td>@item.Nickname</td>
                            <td>@item.Name</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <script type="text/javascript">
        jQuery(function ($) {
            $('.table-sortable').footable();
        });
    </script>
}